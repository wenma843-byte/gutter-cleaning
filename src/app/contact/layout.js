export const metadata = {
  title: "Get a Quote | Gutter Cleaning",
  description: "Request a free quote for gutter cleaning services.",
};

export default function ContactLayout({ children }) {
  return children;
}
